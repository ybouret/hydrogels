ADD_EXECUTABLE(probe probe.cpp)
TARGET_LINK_YOCTO(probe mk)

ADD_EXECUTABLE(growth growth.cpp)
TARGET_LINK_YOCTO(growth mk)

INSTALL( TARGETS probe  RUNTIME DESTINATION bin )
INSTALL( TARGETS growth RUNTIME DESTINATION bin )


